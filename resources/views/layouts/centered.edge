<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{ title || 'Hypnos' }}</title>
  @if(description)
    <meta name="description" content="{{ description }}">
  @endif
  @if(robots)
    <meta name="robots" content="{{ robots }}">
  @endif
  @entryPointStyles('app')
  @entryPointScripts('app')
</head>
<body class="min-h-screen bg-gray-100 flex p-5 font-inter">
  <main class="max-w-screen-sm w-full m-auto flex flex-col items-center">
    <a href="{{ route('home') }}" class="mb-5">
      <img src="logo-2.png" alt="Logo Hypnos" class="h-16">
    </a>
    @if(flashMessages.has('success') || flashMessages.has('error'))
      @if(flashMessages.has('error'))
        <div class="bg-red-300 border border-red-400 text-red-600 rounded p-4 mb-5 self-stretch flex gap-5 items-center">
          <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" viewBox="0 0 20 20" fill="currentColor">
            <path fill-rule="evenodd" d="M8.257 3.099c.765-1.36 2.722-1.36 3.486 0l5.58 9.92c.75 1.334-.213 2.98-1.742 2.98H4.42c-1.53 0-2.493-1.646-1.743-2.98l5.58-9.92zM11 13a1 1 0 11-2 0 1 1 0 012 0zm-1-8a1 1 0 00-1 1v3a1 1 0 002 0V6a1 1 0 00-1-1z" clip-rule="evenodd" />
          </svg>
          <span>{{ flashMessages.get('error') }}</span>
        </div>
      @else
        <div class="bg-green-300 border border-green-400 text-green-600 rounded p-4 mb-5 self-stretch flex gap-5">
          <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
            <path stroke-linecap="round" stroke-linejoin="round" d="M5 13l4 4L19 7" />
          </svg>
          <span>{{ flashMessages.get('success') }}</span>
        </div>
      @endif
    @endif
    @!section('content')
  </main>
</body>
</html>